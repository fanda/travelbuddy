Dear <%= @user.name %>,

You have been invited on the trip <%= @trip.name %>.

You can accept invitation at <%= AppConfig.app_url.chomp('/') %><%= trip_path @trip %>

Best,

<%= I18n.t('travel_buddy') %>  <%= AppConfig.app_url %>
