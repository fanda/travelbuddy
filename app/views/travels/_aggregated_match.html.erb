<span class="amatch">
    <%= link_to image_tag(facebook_large_picture_url(user.facebook_id),
                :title => "#{user.name} #{strip_tags(message)}"),
                user_profile_url(user.facebook_id), :class=>'bimg' %>
    <%= link_to user.name,
                user_profile_url(user.facebook_id), :class=>'name' %>
</span>
